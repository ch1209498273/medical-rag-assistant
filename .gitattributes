# Keep source and documentation diffs stable across Windows and CI runners.
* text=auto eol=lf

# Generated public demo containers are binary artifacts, not text diffs.
*.docx binary
*.gif binary
*.pdf binary
*.png binary
