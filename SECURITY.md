# 安全与隐私边界

医疗知识问答助手是本机优先的工程作品集，不是托管公网服务。v1.0 只使用四份原创虚构资料做演示；这些资料不代表真实企业制度、医保规则、诊疗建议或临床决策支持。

## 凭据规则

- Demo 模式不读取 DeepSeek、SiliconFlow 或其他 Provider Key，也不访问外网；`scripts/demo_smoke.py --no-network` 会用拒绝外呼的 transport 验证这一点。
- Cloud 模式必须由运行者显式选择，并只读取运行者本机 `.env` 中自备的 DeepSeek/SiliconFlow Key。缺少必需 Key 时，后端在启动前 fail closed，不切换到 Demo、其他账户、其他平台或收费模型。
- 前端没有 Key 输入框。Key 不应进入浏览器、HTML、JavaScript bundle、localStorage、SSE、错误消息、日志、截图、提交或公开历史。
- `.env.example` 只有空字段和安全默认值；不要把可调用的 Key 或看似真实的 token 写进示例、测试或 issue。

## 资料与运行环境

不得把真实内部资料、患者信息、私有评测问题/答案、Provider 原始响应、本机绝对路径、用户名、SQLite 文件、Qdrant 数据、日志或私人手册复制到公开副本。公开导出通过显式白名单生成，独立副本还必须执行文件、文本和历史扫描。

运行脚本只绑定本机地址。`data/runtime/` 下的 Demo/Cloud 数据是可删除的本机运行状态，不是公开资料目录；停止脚本只验证启动器记录的进程所有权后才停止进程。

## 漏洞报告

请不要在公开 issue 或聊天中粘贴 Key、Authorization/Bearer 值、真实资料、患者信息、私有问题/答案或完整 Provider 响应。先删除敏感内容，只提交可复现的最小描述、受影响组件、预期/实际行为和修复建议；无法脱敏时请通过项目维护者的私下渠道联系。此项目没有在线用户账户或公共 API，因此不要把本地配置发给维护者。

如果 Key 曾经进入任何公开 Git 历史，应立即在对应 Provider 处撤销并轮换，然后再处理历史清理；“后来删除文件”不等于凭据安全。Task 10 不会自动重写本地私有仓库历史，也不会替用户执行凭据轮换。

## 诚实声明

安全扫描只能证明扫描范围内的文件、文本和历史没有命中规则，不能证明医疗正确性、合规性、渗透测试覆盖或生产安全。v1.0 尚无登录、ACL、多用户隔离、审计后台或公网部署；请仅在受信任的本机演示环境运行。
